name: example_load_postgres
frequency: one-time
load_time: 19:19  # Hour:Min in UTC

description: Example for the load_postgres step

steps:
-   step_type: extract-s3
    file_uri: s3://optimus-etl-data/dev/test_table.tsv

-   step_type: load-postgres
    table: test_table
    insert_query: 'INSERT INTO test_table (poc_type,week,price,carname,dealername,phone,typeseller,additional_data) VALUES (?,?,?,?,?,?,?,?)'
